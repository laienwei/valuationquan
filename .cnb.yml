master:
  push:
    - env:
        # 直接声明静态字符串变量（值用双引号包裹）
        MY_STATIC_VAR: insertvalue
      stages:
      - name: 定义静态字符串变量
        script: |
          echo "静态字符串变量值：${MY_STATIC_VAR}"
