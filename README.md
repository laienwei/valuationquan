###开发目标：
很显然，如果希望得到数据文件，那么网页端的AI是不能完成任务的，必须开发相应的AI agent，但考虑到对数据文件格式暂没有特别的要求，所以选了相对常见的csv格式。
###推送说明：
1月7日首次运行aiagent.py并未取得成功，纵然已在命令行下生成了result.csv。原因是AI的回复并不是每次都带有一个一模一样的'### 说明：'，所以修正以后加入了以'|'标志来判断是否数据已全部给出。然后得到了newresult.csv。
1月13日，对首尾两个'|'符号的提取逻辑都顺利调整完毕，至于reslai1[0]为何会包含数据表格的标题行，仍有待考证。生成的temecv2.csv没有保存上传。
1月23日，对首行的提取逻辑进行了调整。。
2月12日，测试了ssh登陆后的远程开发，以及在本地退出登陆甚至本地系统重启后的ssh连接状态，同时增加了txt文件品种。
